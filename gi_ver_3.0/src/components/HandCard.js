import React from 'react';

function HandCard(props) {
    return (
        <div id="handcards2" class="cardzone">
            <h1>handcards {props.num}</h1>
        </div>

    );
}

export default HandCard;